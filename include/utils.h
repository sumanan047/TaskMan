#ifndef UTILS_H
#define UTILS_H

// adds a character to an array of strings
void addStringToArray(char* string, char** array, int size);

// prints the taskman when the program starts
void print_taskman();


#endif // UTILS_H